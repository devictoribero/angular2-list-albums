import AppleMusicClient from '../../AppleMusic/clients/AppleMusicClient';

export default class AlbumClient extends AppleMusicClient {
  private basepath: string = '';
  private provider:
  constructor(provider: AppleMusicClient) {
    this.provider: provider;
  }

  get
}